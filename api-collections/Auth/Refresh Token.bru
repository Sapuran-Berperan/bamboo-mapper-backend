meta {
  name: Refresh Token
  type: http
  seq: 3
}

post {
  url: {{URL}}/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
      "refresh_token": {{Refresh_Token}}
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
